# Agent Harness Test Plan

## Scope

The harness validates agent-native access to project state, smoke checks, report export, and side-effect-free Electron Main task start planning.
It also validates the complete-bid sample generation, template metadata, reference analysis, and project-config validation paths exposed for agents.

## Tests

- `test_core.py`
  - Finds the repository root from the harness package.
  - Builds a structured status payload from real project files.
  - Classifies `plan.md` summary into required pending work, optional enhancement markers, in-progress sections, and blocked external-input items.
  - Builds valid smoke command definitions without running the UI.
  - Lists real Electron Main task definitions through `taskService.cjs`.
  - Builds side-effect-free dry-run task start plans with storage keys, scope ids, and payload signatures.
  - Manages project workspaces through real Electron Main `projectWorkspaceStore.cjs` against isolated temporary `userData`.
  - Generates a generic complete-bid sample `.docx` and build log through the real bid document template and Word builder services.
  - Exports complete-bid template schema and asset mapping through the real bid document template registry.
  - Validates a generic complete-bid project config JSON through the real bid document validation service.
  - Exports Business Bid, AI Evaluation, and Bid Opportunity reports through real Electron Main report builders.
  - Rejects unsupported report kind / format combinations before invoking Node helpers.
- `test_full_e2e.py`
  - Invokes the installed CLI with `--json status`.
  - Invokes the installed CLI with `--json plan-summary` and verifies required work, optional enhancements, generic capability blockers, and sample-document blockers are not collapsed into either a false complete state or a false generic blocked state.
  - Invokes the installed CLI with `--json smoke --check main-syntax` and verifies real Node syntax checks run.
  - Invokes the installed CLI with `--json list-tasks`.
  - Invokes the installed CLI with `--json start-task --type duplicate-analysis --payload-json <path> --dry-run`.
  - Invokes the installed CLI with `--json project-workspace --action list|create|set-active|get-workspace-path --user-data <temp>`.
  - Invokes the installed CLI with `--json bid-document-sample --output-dir <temp> --template-id generic-response`.
  - Invokes the installed CLI with `--json bid-document-analyze-reference --input <sample.docx> --candidate <sample.docx> --output-json <analysis.json>`.
  - Invokes the installed CLI with `--json bid-document-template-info --template-id generic-response --output-json <template-info.json>`.
  - Invokes the installed CLI with `--json bid-document-validate-config --input <project-config.json> --output-json <build-log.json>`.
  - Invokes the installed CLI with `--json export-report --kind rejection` and verifies a Markdown report is generated by the real Electron Main report builder.
  - Invokes the installed CLI with `--json export-report --kind rejection --format docx` and verifies a Word report is generated by the real Electron Main report builder.
  - Invokes the installed CLI with `--json export-report --kind rejection --format pdf` and verifies a PDF report is generated by the real Electron Main report builder.
  - Invokes the installed CLI with `--json export-report --kind business-bid --format xlsx` and verifies an Excel report is generated by the real Electron Main report builder.

## Validation Log

2026-06-15:

```bash
python3 -m venv agent-harness/.venv
agent-harness/.venv/bin/python -m pip install -e agent-harness
agent-harness/.venv/bin/python -m unittest discover -s agent-harness/cli_anything/openbidkit_yibiao/tests -v
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json status
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json plan-summary
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json smoke --check main-syntax --timeout 60
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json list-tasks
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json start-task --type duplicate-analysis --payload-json /tmp/task-payload.json --dry-run
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json project-workspace --action list --user-data /tmp/yibiao-agent-user-data
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json project-workspace --action create --name "CLI 测试项目" --make-active --user-data /tmp/yibiao-agent-user-data
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json bid-document-sample --output-dir /tmp/yibiao-bid-document --template-id generic-response
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json bid-document-analyze-reference --input /tmp/yibiao-bid-document/generic-response-sample.docx --candidate /tmp/yibiao-bid-document/generic-response-sample.docx --output-json /tmp/yibiao-bid-document/reference-alignment.json
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json export-report --kind duplicate --state-json /tmp/duplicate-state.json --output /tmp/duplicate-report.md
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json export-report --kind rejection --state-json /tmp/rejection-state.json --output /tmp/rejection-report.docx --format docx
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json export-report --kind rejection --state-json /tmp/rejection-state.json --output /tmp/rejection-report.pdf --format pdf
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json export-report --kind business-bid --state-json /tmp/business-bid-state.json --output /tmp/business-bid-report.xlsx --format xlsx
```

Result: all 28 unittest cases passed, including installed CLI subprocess E2E, plan-summary required/optional work classification, real `node --check electron/services/fileService.cjs` smoke, project workspace list/create/switch/path commands through real Electron Main project workspace store, complete-bid sample docx/build-log generation, reference docx structure analysis/alignment, duplicate/rejection Markdown, Word, plus PDF report export, business-bid Excel E2E export, Business Bid / AI Evaluation / Bid Opportunity core exports through Electron Main report builders, unsupported format rejection, and dry-run task planning through real Electron Main task definitions.

2026-06-18:

```bash
node --check agent-harness/cli_anything/openbidkit_yibiao/scripts/bid_document_validate_config.cjs
agent-harness/.venv/bin/python -m unittest discover -s agent-harness/cli_anything/openbidkit_yibiao/tests -v
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json bid-document-validate-config --input output/bid-document-samples/generic-response-project-config.json --output-json output/bid-document-samples/generic-response-project-config.validation.json
```

Result: all 32 unittest cases passed, including installed CLI subprocess E2E for `bid-document-validate-config`. The persisted validation artifact reports `ok: true`, `quote_total: 300`, `target_total: 300`, passing quote, payment, forbidden-word, asset, and required-section checks for the generic-response project config.

2026-06-19:

```bash
node --check agent-harness/cli_anything/openbidkit_yibiao/scripts/bid_document_readiness_report.cjs
agent-harness/.venv/bin/python -m unittest discover -s agent-harness/cli_anything/openbidkit_yibiao/tests -v
agent-harness/.venv/bin/cli-anything-openbidkit-yibiao --json bid-document-readiness-report --input output/bid-document-samples/smart-canteen-init-config.json --output-json output/bid-document-samples/smart-canteen-readiness.json --output-markdown output/bid-document-samples/smart-canteen-readiness.md --output-xlsx output/bid-document-samples/smart-canteen-readiness.xlsx
```

Result: readiness report export supports JSON, Markdown, and Excel `.xlsx` outputs. The Excel workbook contains overview, blockers, missing assets, and check-status sheets for business-side collection, while preserving the same non-zero exit behavior when blockers remain.
