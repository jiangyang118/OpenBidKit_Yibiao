# 易标投标工具箱用户手册与故障排查

本文用于给用户、测试人员和后续 Codex 快速确认核心功能入口、交付物和常见问题处理方式。本文只描述当前已落地能力；多项目运行时热刷新、真实图片截屏证据和复杂相似图证据视图仍按 `plan.md` 后续迭代。

## 1. 技术方案与已有方案扩写

- 入口：左侧主菜单 `生成技术方案`、`已有方案扩写`。
- 输入：招标文件；已有方案扩写额外导入原方案。
- 核心流程：招标文件解析 -> 招标要求分析 -> 目录生成/编辑 -> 全局事实 -> 正文生成/编辑。
- 交付：正文 Markdown 预览、图片/ Mermaid 预览、Word 导出。
- 注意：目录重新生成、编辑、添加或删除后，旧正文内容和正文生成缓存会被清空，避免目录与正文错位。

常见问题：

- 导入文件失败：先到 `设置 -> 文件解析` 检查本地解析 / MinerU 配置；扫描件、图片和 OFD 优先走 MinerU 或先转换。
- Word 图片缺失：导出会给出 warnings；缺失本地图片不会阻断 docx 生成，请按 warnings 回到正文图片来源修复。
- 任务中断：重新进入页面后先读取 Store 和 active task；如任务已无 active task，应重新执行对应步骤。

## 2. 商务标

- 入口：`标书制作 -> 商务标`。
- 输入：可复用技术方案招标文件，也可独立导入商务标招标文件。
- 核心能力：商务响应矩阵、合同偏离、资信材料、报价附件、负责人/确认人、AI 结构化提取、独立附件管理。
- 交付：Markdown、Word `.docx`、Excel `.xlsx` 商务响应交付包。

常见问题：

- AI 结构化提取失败：系统保留已有矩阵，不会清空；可继续使用规则提取结果并人工编辑。
- 附件删除后仍显示：刷新页面读取 Store；如文件本体已被清理，重新导入附件。

## 3. AI 评标

- 入口：`标书制作 -> AI 评标`。
- 输入：评分办法/招标文件、投标文件。
- 核心能力：评分项抽取、投标文件证据匹配、多投标文件评分、批量重评、专家打分、审计意见。
- 交付：Markdown 自评报告、Word `.docx` 正式报告、Excel `.xlsx` 多工作表报告。

常见问题：

- 投标文件证据缺失：检查导入的 Markdown 原文是否包含评分项关键词；证据字段会尽量携带章节、行号和命中关键词。
- 专家分差异常：系统会生成专家间分差和专家均分偏差审计意见，需人工复核后调整最终分。

## 4. 投标机会

- 入口：左侧主菜单 `投标机会`。
- 输入：手动录入公告、导入公告文件或输入公告 URL。
- 核心能力：规则/AI 结构化解析、机会评分、经营维度拆解、负责人、下一步动作、提醒时间、知识库匹配。
- 交付：Markdown 投标建议报告、`.ics` 日历提醒。

常见问题：

- URL 导入内容为空：网站可能反爬或需要登录；可保存网页为文件后导入。
- 日历导出缺少机会：只有设置了提醒时间的机会会进入 iCalendar。

## 5. 知识库与图片知识库

- 文档知识库入口：`知识库`。
- 图片知识库入口：`资源与素材 -> 图片知识库`。
- 文档知识库能力：文档迁移、解析、分析、匹配、active task 恢复。
- 图片知识库能力：上传、分类、文件夹、标签、批量管理、正文插入、引用回溯、正文生成优先复用。

常见问题：

- 旧知识库迁移提示：只迁移旧版 `status = success` 的文档；未完成或处理中旧文档不会迁移。
- 图片无法导出到 Word：确认图片仍存在于本地工作区，正文引用应为 `yibiao-asset://image-knowledge-base/...` 或可解析的本地资源。

## 6. 标书查重

- 入口：`标书检查 -> 标书查重`。
- 输入：可选招标文件，多份投标文件。
- 核心能力：元数据、目录、正文重复句、完全重复图片、感知 hash 相似图片。
- 人工处理：确认重复、忽略、恢复待处理、批量处理、正文常用忽略规则。
- 交付：Markdown / Word / PDF 查重报告，包含批量处理建议、人工状态和重复/相似图片证据；PDF 当前为文本型报告，截图证据视图后续增强。

常见问题：

- 招标文件引用句被误判：导入招标文件后会作为白名单过滤；也可把固定模板句加入正文忽略规则。
- 相似图片没有命中：当前相似图片第一阶段主要覆盖压缩、缩放；局部裁剪、旋转、水印遮挡仍需人工复核。
- 报告内容和页面不一致：确认/忽略/删除后重新导出报告，报告以 Main 侧 Store 当前状态为准。

## 7. 废标项检查

- 入口：`标书检查 -> 废标项检查`。
- 输入：招标文件、投标文件；可从技术方案读取招标文件。
- 核心能力：无效与废标项解析、废标风险检查、错别字检查、逻辑谬误检查、单投标文件重查。
- 人工处理：忽略、恢复、删除、批量处理、复制证据。
- 交付：Markdown / Word / PDF 废标项检查报告，包含证据定位索引、章节/行号/前后文提示和文本型证据截图视图；PDF 当前为文本型报告，真实图片截屏证据后续增强。

常见问题：

- 证据无法精确定位：报告会保留原文证据和人工检索提示；请回到对应投标文件正文搜索。
- 只想重查一份投标文件：在结果页筛选到具体投标文件后使用“重新检查当前投标文件”。

## 8. 设置、资源与开发者工具

- 设置：配置文本模型、生图模型、文件解析、主题、布局、更新渠道。
- 资源下载：读取 Analytics 资源接口；接口失败时优先展示本地缓存，仍无缓存时展示友好空态。
- 开发者工具：Prompt 调试台、文件解析沙盘、导出预演、JSON 请求实验室。

常见问题：

- 本地模型没有 API Key：LM Studio / Ollama 等本地服务允许无 API Key；先确认 Base URL 和模型列表。
- 资源加载失败：检查网络或服务端资源接口；客户端失败不影响其他主流程。

## 9. 本地验证命令

客户端验证：

```powershell
cd client
npm run test:unit
npm run build
npm run test:e2e
```

Electron Main / preload 语法检查示例：

```powershell
cd client
node --check electron\preload.cjs
node --check electron\services\duplicateCheckService.cjs
node --check electron\services\rejectionCheckStore.cjs
```

Analytics 验证：

```powershell
cd analytics\worker
npm run check
npm run test

cd analytics\dashboard
npm run check
npm run test
```

## 10. 已知边界

- 设置页已提供项目工作区列表、新建并切换、切换确认、归档/恢复和删除入口；切换或删除前会阻止后台任务运行中的操作。切换项目后需要重启应用加载对应工作区，运行时热刷新和更完整跨项目隔离验收仍未完成。
- 查重和废标检查均已支持 Markdown / Word / PDF 文本报告；废标项检查报告已包含文本型证据截图视图，真实图片截屏证据和复杂相似图证据视图仍未完成。
- 文件解析能力已有支持矩阵、解析沙盘和本地真实样本文档回归；MinerU 精准 / MinerU Agent 的真实网络端到端回归仍需在具备 Token 和网络的环境中补跑固化。
